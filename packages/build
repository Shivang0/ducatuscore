#!/bin/sh

MODULES=' crypto-wallet-core ducatuscore-wallet-service ducatuscore-wallet-client ducatuscore-client'

for i in $MODULES; 
do
  echo "## $i"
  cd $i && npm run compile || (echo "ERROR" && exit) && cd ..
done
