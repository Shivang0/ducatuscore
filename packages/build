#!/bin/sh

MODULES=' ducatus-crypto-wallet-core-rev  ducatus-wallet-service-rev ducatus-wallet-client'

for i in $MODULES; 
do
  echo "## $i"
  cd $i && npm run compile || (echo "ERROR" && exit) && cd ..
done
